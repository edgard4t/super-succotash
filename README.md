# Super Succotash 

Simple task for upcoming young web developer

Create a simple C program that makes a **HTTP** *request* to 'https://app.4tgestor.com.br/api' and prints out the *body* of the *response*.

The follwing command must not return an error and needs to show the correct output.

```
make
```